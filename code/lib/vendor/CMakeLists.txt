# Add third-party libraries here and expose them as normal CMake targets.
#
# Example:
# add_library(vendor_fmt STATIC
#     fmt/src/format.cc
# )
# target_include_directories(vendor_fmt PUBLIC fmt/include)
