function(dic_apply_target_overrides)
    # Manual target wiring belongs here.
    #
    # Example:
    # if(TARGET hw3_impl AND TARGET vendor_fmt)
    #     target_link_libraries(hw3_impl PUBLIC vendor_fmt)
    # endif()
endfunction()
